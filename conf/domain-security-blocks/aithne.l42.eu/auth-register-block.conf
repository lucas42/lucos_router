  # SECURITY BLOCK: WebAuthn registration paths blocked pending lucas42/lucos_aithne#10 enrolment guard.
  # Without the guard, unauthenticated callers can register passkeys for any contact_id.
  # Remove this file once lucas42/lucos_aithne#10 is deployed to production.
  location /auth/register/ {
    return 503;
  }
